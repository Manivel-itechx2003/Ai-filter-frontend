const Header = () => (
  <div className="text-center mb-6">
    <h1 className="text-4xl font-bold text-blue-600">Smart Resume Analyzer</h1>
    <p className="text-gray-600 mt-2">Upload resume(s) & job description to get match scores</p>
  </div>
);

export default Header;
